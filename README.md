Costco app

## Run locally (Expo)

install project dependencies:
```bash
npm install
```
start expo:
```bash
npx expo start
```

Then open the iOS simulator, Android emulator, or scan the QR code with Expo Go.
